# Application URL
app.base-url=http://localhost:8082
app.admin.email=admin@example.com
app.admin.password=Admin@123
app.client.email=client@example.com
app.client.password=Client@123
# Server Configuration
server.port=8082

# Application
spring.application.name=consulting-platform
spring.main.allow-bean-definition-overriding=true
spring.profiles.active=mock
#spring.profiles.active=live

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/consulting_platform?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=rajesh
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.hibernate.ddl-auto=validate

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kubdegulshan08@gmail.com
spring.mail.password=vged vnjy xppu dsol
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# JWT Configuration
app.jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
app.jwt.expiration-ms=86400000
app.jwt.refresh-expiration-ms=604800000

# Security
app.security.verification-token.expiration-minutes=1440
app.security.password-reset-token.expiration-minutes=30

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000,http://localhost:3001,http://localhost:5174
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=Authorization,Content-Type,Accept,X-Requested-With
app.cors.allow-credentials=true

# Logging
logging.level.root=INFO
logging.level.org.springframework=INFO
logging.level.com.elevate.consultingplatform=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
# Write logs to a file for easier runtime inspection
logging.file.name=logs/consulting-platform.log

# Client app URL used to build links in emails (reset password / verify email)
app.client.base-url=http://localhost:3001

