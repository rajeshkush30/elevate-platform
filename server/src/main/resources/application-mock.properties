# Mock profile configuration. Safe defaults for local/dev. Use with: --spring.profiles.active=mock

# External services (mock beans will ignore these, but keep for completeness)
ai.baseUrl=http://localhost:8000

lms.baseUrl=http://localhost:9000
lms.apiKey=mock-key

crm.zoho.baseUrl=https://www.zohoapis.com
crm.zoho.accessToken=mock-token

# App URLs for emails (used by EmailServiceImpl)
app.base-url=http://localhost:3001
app.client.base-url=http://localhost:3001
